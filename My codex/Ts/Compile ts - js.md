VSCODE terminal cmd: 
```bash
tsc --init : tạo tsconfig.json
```
target:"es2016" compiler ts thành js version nào,
outDir"./dist": cmp xong gom vô folder dist

Ctrl Shift B 
=> build là viết code mới thì phải build lại để tạo ra js trong dir để đồng bộ
=> watch là cứ có code mới thì vscode tự build
